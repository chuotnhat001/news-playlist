---
phase: 02-ui-user-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/features/home/home_screen.dart
  - lib/features/home/widgets/category_card.dart
  - lib/features/playlist/playlist_screen.dart
  - lib/features/playlist/widgets/article_tile.dart
  - lib/providers/content_provider.dart
autonomous: true
requirements: [R3]
must_haves:
  truths:
    - "User sees a grid of news categories on the Home screen"
    - "User taps a category and navigates to the Playlist screen"
    - "Playlist screen shows list of articles for the selected category"
    - "User taps Play All and playback starts with the full article list"
  artifacts:
    - path: "lib/features/home/home_screen.dart"
      provides: "Category grid with tap navigation"
      min_lines: 40
    - path: "lib/features/playlist/playlist_screen.dart"
      provides: "Article list with Play All button"
      min_lines: 50
    - path: "lib/providers/content_provider.dart"
      provides: "Riverpod providers for ContentService"
      min_lines: 20
  key_links:
    - from: "lib/features/home/home_screen.dart"
      to: "lib/features/playlist/playlist_screen.dart"
      via: "Navigator.push with category argument"
      pattern: "Navigator\\.push"
    - from: "lib/features/playlist/playlist_screen.dart"
      to: "lib/providers/audio_player_provider.dart"
      via: "ref.read(audioPlayerProvider.notifier).setPlaylist"
      pattern: "setPlaylist"
---

<objective>
Build the Home screen (category grid) and Playlist screen (article list with Play All) with navigation between them.

Purpose: Establish the primary user flow — browsing categories and starting playlist playback.
Output: Two functional screens connected via Navigator, wired to ContentService and AudioPlayerProvider.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/models/article.dart
@lib/services/content_service.dart
@lib/providers/audio_player_provider.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content provider and Home screen with category grid</name>
  <files>
    lib/providers/content_provider.dart,
    lib/features/home/home_screen.dart,
    lib/features/home/widgets/category_card.dart,
    lib/main.dart
  </files>
  <action>
Create lib/providers/content_provider.dart:
- Define a contentServiceProvider (Provider) that creates ContentService with CacheService and crawler map (dantri, soha) — reuse existing service classes.
- Define a categoryListProvider that returns the static list of categories from ContentService.categoryUrls.keys (cong-nghe, kinh-doanh, chung-khoan).
- Define an articlesProvider(String category) as a FutureProvider.family that calls contentService.getArticles(category).

Create lib/features/home/home_screen.dart:
- ConsumerWidget with Scaffold, AppBar title "News Playlist".
- Body: GridView.builder with 2 columns showing CategoryCard for each category.
- Each CategoryCard taps to Navigator.push to PlaylistScreen(category: category).

Create lib/features/home/widgets/category_card.dart:
- StatelessWidget taking category String.
- Material 3 Card with category name (formatted: replace hyphens with spaces, capitalize), an icon (Icons.computer for cong-nghe, Icons.business for kinh-doanh, Icons.show_chart for chung-khoan).
- InkWell with onTap callback.

Update lib/main.dart:
- Set home to HomeScreen().
- Add ProviderScope initialization for contentServiceProvider (call init() via a FutureProvider or in a Startup widget).
  </action>
  <verify>
    <automated>cd "D:/ClaudeCode/projects/News Playlist" && flutter analyze</automated>
  </verify>
  <done>Home screen renders category grid. Tapping a category navigates to PlaylistScreen. No analysis errors.</done>
</task>

<task type="auto">
  <name>Task 2: Playlist screen with article list and Play All</name>
  <files>
    lib/features/playlist/playlist_screen.dart,
    lib/features/playlist/widgets/article_tile.dart
  </files>
  <action>
Create lib/features/playlist/playlist_screen.dart:
- ConsumerWidget receiving category String via constructor.
- Use ref.watch on articlesProvider(category) to get AsyncValue of articles.
- AppBar title: formatted category name.
- Body: AsyncValue.when pattern — loading: CircularProgressIndicator centered, error: error text, data: article list.
- FloatingActionButton or top-bar ElevatedButton labeled "Play All" — onPressed calls ref.read(audioPlayerProvider.notifier).setPlaylist(articles).
- ListView.builder with ArticleTile for each article.

Create lib/features/playlist/widgets/article_tile.dart:
- StatelessWidget taking Article.
- ListTile with title (article.title, maxLines 2, overflow ellipsis), subtitle (article.source + formatted publishedAt), leading icon (Icons.audiotrack).
- No individual tap action yet (Play All is the primary action).
  </action>
  <verify>
    <automated>cd "D:/ClaudeCode/projects/News Playlist" && flutter analyze</automated>
  </verify>
  <done>Playlist screen loads articles for selected category, displays them in a list. Play All button calls setPlaylist on AudioPlayerProvider. No analysis errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContentService → Network | HTTP requests to external news sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | content_provider | accept | ContentService already handles network errors with fallback to cache |
| T-02-02 | Denial of Service | articlesProvider | mitigate | AsyncValue loading state prevents UI freeze; FutureProvider handles timeout via ContentService |
</threat_model>

<verification>
- flutter analyze passes with zero errors
- App launches, Home screen shows 3 category cards
- Tapping a category navigates to Playlist screen
- Playlist screen shows loading then article list (or empty if no network)
- Play All button triggers audio playback
</verification>

<success_criteria>
- Home screen renders category grid with 3 categories
- Navigation from Home to Playlist works via tap
- Playlist screen fetches and displays articles
- Play All starts playlist playback via AudioPlayerProvider
- Zero flutter analyze errors
</success_criteria>

<output>
Create `.planning/phases/02-ui-user-experience/02-01-SUMMARY.md` when done
</output>
