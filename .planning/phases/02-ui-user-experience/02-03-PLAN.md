---
phase: 02-ui-user-experience
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - lib/main.dart
  - lib/features/home/home_screen.dart
  - lib/features/playlist/playlist_screen.dart
  - lib/features/playlist/widgets/article_tile.dart
  - lib/features/player/mini_player.dart
  - lib/shared/widgets/empty_state.dart
  - lib/shared/widgets/error_toast.dart
autonomous: true
requirements: [R6]
must_haves:
  truths:
    - "When no articles exist for a category, user sees a friendly empty state with illustration and message"
    - "When loading articles, user sees a shimmer/skeleton placeholder"
    - "User can pull-to-refresh the playlist screen to re-fetch articles"
    - "When a playback or network error occurs, a snackbar toast appears briefly"
    - "Navigation wiring is complete: main.dart uses AppShell, Home → Playlist → Player flow is seamless"
  artifacts:
    - path: "lib/shared/widgets/empty_state.dart"
      provides: "Reusable empty state widget with icon, title, subtitle"
      min_lines: 25
    - path: "lib/shared/widgets/error_toast.dart"
      provides: "Helper function to show error SnackBar"
      min_lines: 10
    - path: "lib/features/playlist/playlist_screen.dart"
      provides: "Pull-to-refresh and loading/empty states integrated"
      min_lines: 60
  key_links:
    - from: "lib/main.dart"
      to: "lib/app_shell.dart"
      via: "AppShell wraps Navigator/HomeScreen"
      pattern: "AppShell"
    - from: "lib/features/playlist/playlist_screen.dart"
      to: "lib/shared/widgets/empty_state.dart"
      via: "Displayed when articles list is empty"
      pattern: "EmptyState"
    - from: "lib/features/playlist/playlist_screen.dart"
      to: "lib/shared/widgets/error_toast.dart"
      via: "Shown on refresh error"
      pattern: "showErrorToast"
---

<objective>
Polish the UI with empty states, loading indicators, pull-to-refresh, error toasts, and final navigation wiring.

Purpose: Ensure all edge cases are handled gracefully and the full user flow works end-to-end from app launch through playback.
Output: Shared widgets for empty/error states, loading shimmer, RefreshIndicator integration, AppShell wiring in main.dart.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/main.dart
@lib/app_shell.dart
@lib/features/home/home_screen.dart
@lib/features/playlist/playlist_screen.dart
@lib/features/player/mini_player.dart
@lib/providers/audio_player_provider.dart
@lib/providers/content_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared widgets (empty state + error toast) and loading states</name>
  <files>
    lib/shared/widgets/empty_state.dart,
    lib/shared/widgets/error_toast.dart
  </files>
  <action>
Create lib/shared/widgets/empty_state.dart:
- StatelessWidget taking: IconData icon, String title, String subtitle (optional), VoidCallback? onAction, String? actionLabel.
- Renders a centered Column: Icon (size 64, color secondary), SizedBox(height: 16), Text title (style titleMedium), if subtitle: Text subtitle (style bodyMedium, color onSurfaceVariant), if onAction + actionLabel: SizedBox(height: 24) + OutlinedButton with actionLabel text.
- Use Material 3 theme colors throughout.

Create lib/shared/widgets/error_toast.dart:
- Top-level function: void showErrorToast(BuildContext context, String message).
- Implementation: ScaffoldMessenger.of(context).showSnackBar with SnackBar(content: Text(message), behavior: SnackBarBehavior.floating, duration: Duration(seconds: 3), action: SnackBarAction(label: 'Dismiss', onPressed: () => ScaffoldMessenger.of(context).hideCurrentSnackBar())).
  </action>
  <verify>
    <automated>cd "D:/ClaudeCode/projects/News Playlist" && flutter analyze</automated>
  </verify>
  <done>EmptyState widget and showErrorToast function exist. Both compile cleanly with zero analysis errors.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate empty states, loading, pull-to-refresh, error toasts, and navigation wiring</name>
  <files>
    lib/features/playlist/playlist_screen.dart,
    lib/features/home/home_screen.dart,
    lib/main.dart
  </files>
  <action>
Update lib/features/playlist/playlist_screen.dart:
- Wrap the ListView.builder in a RefreshIndicator. The onRefresh callback calls ref.read(contentServiceProvider).refreshCategory(category) then invalidates the articlesProvider(category) to refetch.
- In the AsyncValue.when data handler: if articles.isEmpty, show EmptyState(icon: Icons.article_outlined, title: 'No articles found', subtitle: 'Pull down to refresh', onAction: refresh callback, actionLabel: 'Refresh').
- In the loading state: show a Column of 5 ListTile-shaped shimmer placeholders (Container with decoration BoxDecoration color grey.shade200, rounded, with fixed heights to simulate title/subtitle layout). Keep it simple — no external shimmer package, just animated opacity or static grey boxes.
- In the error handler: show EmptyState with error icon + message, and also call showErrorToast(context, error.toString()) via a ref.listen or post-frame callback (avoid calling during build).
- When Play All is tapped and playback error occurs (listen to audioPlayerProvider error field via ref.listen), show showErrorToast with the error message.

Update lib/features/home/home_screen.dart:
- No loading state needed (categories are static), but ensure graceful display.

Update lib/main.dart:
- Replace the placeholder Scaffold(body: Center(child: Text)) with AppShell wrapping HomeScreen.
- Ensure the Navigator is inside AppShell so that pushed routes (PlaylistScreen, PlayerScreen) still show the MiniPlayer at the bottom.
- Structure: MaterialApp home → AppShell(child: Navigator or direct HomeScreen), where AppShell's child is the content area above MiniPlayer.
- Use a GlobalKey for Navigator if needed, or simply have AppShell wrap HomeScreen and let Navigator.push create routes that overlay within the Expanded area above MiniPlayer.
  </action>
  <verify>
    <automated>cd "D:/ClaudeCode/projects/News Playlist" && flutter analyze</automated>
  </verify>
  <done>Full navigation flow works: main.dart launches AppShell with HomeScreen, mini player persists. Playlist has pull-to-refresh, empty state when no articles, loading placeholders, and error toasts on failure. Zero analysis errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → UI | Network errors surface as user-facing toasts |
| AudioService → UI | Playback errors surface as user-facing toasts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | error_toast | mitigate | Show user-friendly messages, not raw stack traces; error.toString() is acceptable for network errors but not for internal exceptions |
| T-02-06 | Denial of Service | pull-to-refresh | mitigate | RefreshIndicator has built-in debounce; ContentService falls back to cache on network failure |
</threat_model>

<verification>
- flutter analyze passes with zero errors
- App launches to Home screen with category grid
- Tapping category navigates to Playlist with loading state visible briefly
- If no articles: empty state displays with refresh button
- Pull-to-refresh triggers re-fetch
- Play All starts playback, mini player appears
- Tapping mini player opens full player
- Playback errors show toast notification
- Mini player persists when navigating back to Home
</verification>

<success_criteria>
- Complete navigation flow: Home → Playlist → Player (mini + full)
- Empty states shown when no articles available
- Loading placeholders shown during fetch
- Pull-to-refresh functional on Playlist screen
- Error toasts appear on network/playback failures
- Mini player persists across all screen navigations
- Zero flutter analyze errors
</success_criteria>

<output>
Create `.planning/phases/02-ui-user-experience/02-03-SUMMARY.md` when done
</output>
