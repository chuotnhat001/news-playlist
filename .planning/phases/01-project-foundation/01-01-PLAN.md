---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/models/article.dart
  - test/models/article_test.dart
autonomous: true
requirements: [R1, R2, R4]

must_haves:
  truths:
    - "Flutter project compiles and runs"
    - "All required dependencies resolve without conflict"
    - "Article data class can serialize to/from SQLite map"
    - "Feature-first folder structure exists"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Project configuration with all dependencies"
      contains: "flutter_riverpod"
    - path: "lib/main.dart"
      provides: "App entry point with Riverpod ProviderScope"
      contains: "ProviderScope"
    - path: "lib/models/article.dart"
      provides: "Article data class with SQLite serialization"
      exports: ["Article"]
    - path: "test/models/article_test.dart"
      provides: "Unit tests for Article model"
      min_lines: 40
  key_links:
    - from: "lib/main.dart"
      to: "flutter_riverpod"
      via: "ProviderScope wrapper"
      pattern: "ProviderScope"
    - from: "lib/models/article.dart"
      to: "SQLite schema"
      via: "toMap/fromMap methods"
      pattern: "toMap|fromMap"
---

<objective>
Bootstrap the Flutter project with all dependencies, feature-first folder structure, Riverpod app shell, and the Article model that all other services depend on.

Purpose: Every subsequent plan depends on the project scaffold and Article model. This is the foundation layer.
Output: Compilable Flutter project with Article model passing unit tests.
</objective>

<execution_context>
@C:/Users/Admin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flutter project init with dependencies and folder structure</name>
  <files>pubspec.yaml, lib/main.dart, lib/features/home/.gitkeep, lib/features/playlist/.gitkeep, lib/features/player/.gitkeep, lib/services/.gitkeep, lib/models/.gitkeep, lib/providers/.gitkeep</files>
  <action>
  Initialize Flutter project (flutter create --org com.newsplaylist --project-name news_playlist . OR use existing if pubspec.yaml already present).

  Update pubspec.yaml dependencies:
  - flutter_riverpod: ^2.4.0
  - dio: ^5.4.0
  - html: ^0.15.4
  - sqflite: ^2.3.0
  - path_provider: ^2.1.0
  - just_audio: ^0.9.36
  - audio_service: ^0.18.12

  Add dev_dependencies:
  - mockito: ^5.4.0
  - build_runner: ^2.4.0

  Create feature-first folder structure:
  - lib/features/home/
  - lib/features/playlist/
  - lib/features/player/
  - lib/services/
  - lib/models/
  - lib/providers/

  Add .gitkeep to empty directories.

  Update lib/main.dart:
  - Import flutter_riverpod
  - Wrap app in ProviderScope
  - Create NewsPlaylistApp as MaterialApp with Vietnamese locale title "News Playlist"
  - Use Material 3 theme

  Run flutter pub get to verify dependency resolution. Run flutter analyze to confirm zero errors.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze</automated>
  </verify>
  <done>Flutter project compiles. All dependencies resolve. Feature-first folder structure exists. main.dart wraps app in ProviderScope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Article model with SQLite serialization</name>
  <files>lib/models/article.dart, test/models/article_test.dart</files>
  <behavior>
  - toMap() produces Map with all fields, DateTime stored as millisecondsSinceEpoch (int)
  - fromMap() reconstructs Article from Map with correct types
  - Roundtrip: Article -> toMap() -> fromMap() == original Article
  - copyWith() returns new instance with specified fields changed, others preserved
  - Equality: two Articles with same id are equal regardless of other fields
  - hashCode: consistent with equality (based on id)
  </behavior>
  <action>
  Create lib/models/article.dart with immutable Article class:
  - Fields: id (String), title (String), source (String — 'dantri' or 'soha'), audioUrl (String), articleUrl (String), category (String), publishedAt (DateTime), cachedAt (DateTime)
  - toMap() method: converts to Map for SQLite (DateTime as millisecondsSinceEpoch)
  - fromMap(Map) factory constructor: reconstructs from SQLite row
  - copyWith() method for all fields
  - Override == and hashCode based on id field only
  - Static createTableSQL constant with CREATE TABLE statement:
    id TEXT PRIMARY KEY, title TEXT NOT NULL, source TEXT NOT NULL, audioUrl TEXT NOT NULL, articleUrl TEXT NOT NULL, category TEXT NOT NULL, publishedAt INTEGER NOT NULL, cachedAt INTEGER NOT NULL

  Create test/models/article_test.dart:
  - Test toMap/fromMap roundtrip with all field types
  - Test copyWith preserves unchanged fields
  - Test equality by id
  - Test hashCode consistency
  </action>
  <verify>
    <automated>flutter test test/models/article_test.dart</automated>
  </verify>
  <done>Article model compiles. All unit tests pass. toMap/fromMap are inverse operations. Equality works by id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Pure local scaffolding, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pub dependencies | accept | Dependencies are well-known Flutter packages with high download counts. No RESEARCH.md audit exists; packages chosen per locked architecture decisions. |
</threat_model>

<verification>
- flutter pub get succeeds
- flutter analyze reports zero errors
- flutter test test/models/article_test.dart passes all tests
- Directory structure matches feature-first layout
</verification>

<success_criteria>
- Project compiles and all dependencies resolve
- Article model serializes to/from SQLite-compatible Map
- All Article unit tests pass
- ProviderScope wraps the app in main.dart
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-01-SUMMARY.md` when done
</output>
